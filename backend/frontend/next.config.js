/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',  // Enable static export
  distDir: 'build', // Optional: Specifies the directory for the export
};

module.exports = nextConfig;